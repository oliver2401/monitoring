# disabled temporarily
# apiVersion: kyverno.io/v1
# kind: ClusterPolicy
# metadata:
#   name: require-ro-rootfs
#   annotations:
#     policies.kyverno.io/title: Require Read-Only Root Filesystem
#     policies.kyverno.io/category: Kubernetes Security Hardening
#     policies.kyverno.io/severity: medium
#     policies.kyverno.io/description: >-
#       A read-only root file system helps to enforce an immutable infrastructure strategy;
#       the container only needs to write on the mounted volume that persists the state.
#       An immutable root filesystem can also prevent malicious binaries from writing to the
#       host system. This policy validates that containers define a securityContext
#       with `readOnlyRootFilesystem: true`.      
# spec:
#   validationFailureAction: audit
#   background: true
#   rules:
#   - name: validate-readOnlyRootFilesystem
#     match:
#       any:
#       - resources:
#           kinds:
#           - Pod
#     validate:
#       message: >-
#           [E012]Require Read-Only Root Filesystem
#           (details: my-url/Kyverno+rules#Kyvernorules-[E012]RequireRead-OnlyRootFilesystem)
#       pattern:
#         spec:
#           containers:
#           - securityContext:
#               readOnlyRootFilesystem: true
